import 'package:flutter/material.dart';

import '../../../shared/widgets/app_page_scaffold.dart';

/// Placeholder for web `patientBookAppointmentViaLink` flow.
class PatientBookViaLinkScreen extends StatelessWidget {
  const PatientBookViaLinkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppPageScaffold(title: 'Patient · Book appointment (link)');
  }
}
